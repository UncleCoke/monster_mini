<view class="padding-lg">
    <view class="flex flex-wrap justify-between align-start">
        <view class="cu-card case" wx:for="{{list}}" wx:key="name" bindtap="raceIntroduce" id="{{item.id}}">
            <view class="cu-item shadow card">
                <view class="image">
                    <image src="{{item.eventImage}}" mode="widthFix"></image>
                </view>
                <view class="padding-sm">
                    <view class="text-sp margin-bottom-sm">{{item.title}}</view>
                    <view class="flex align-center justify-between">
                        <view class="text-bold text-red text-sp">{{item.price}}元</view>
                        <button class="cu-btn bg-blue sm round">立即创建</button>
                    </view> 
                </view>
            </view>
        </view>
    </view>
</view>