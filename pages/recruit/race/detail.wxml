<view class="page-sp">
    <view class="bg-white text-sp margin-bottom-sm">
        <view class="flex align-center padding-tb padding-lr-lg">
            <image src="{{imgUrl}}race/icon_race.png" style="width:40rpx;" mode="widthFix" />
            <view class="margin-left-sm text-sp text-bold">{{race.name}}</view>
        </view>
        <view class="padding-lr-lg  padding-top-sm padding-bottom-lg border-bottom">
            <view class="flex align-center justify-between margin-bottom">
                <view>活动时间</view>
            </view>
            <view class="picker-shape middle hover margin-top-sm" style="min-width:560rpx;">
                {{race.startTime}} - {{race.endTime}}
            </view>
        </view>
        <view class="height-sp padding-lr-lg border-bottom flex align-center">
            <view>当前状态</view>
            <view class="padding-left-lg">
                {{race.status == 0?'未开始':(race.status == 1?'进行中':'已结束')}}
            </view>
        </view>
        <view class="flex align-center justify-between" style="padding:40rpx 100rpx">
            <view class="flex align-center justify-center flex-direction">
                <view class="content-shape bg-olive light">{{race.userCount}}</view>
                <view class="text-sm">打开人数</view>
            </view>
            <view class="flex align-center justify-center flex-direction">
                <view class="content-shape bg-greenSecond light">{{race.openCount}}</view>
                <view class="text-sm">打开次数</view>
            </view>
            <view class="flex align-center justify-center flex-direction">
                <view class="content-shape bg-greenThird light">{{race.finishCount}}</view>
                <view class="text-sm">完成人数</view>
            </view>
        </view>
    </view>
    <view class="bg-white text-sp padding-bottom-lg  padding-lr-lg padding-top">
        <view class="flex align-center padding-bottom-sm">
            <image src="{{imgUrl}}eval/icon_report.png" style="width:40rpx;" mode="widthFix" />
            <view class="margin-left-sm text-sp text-bold">活动参与情况</view>
        </view>
        <view class="panel margin-top-sm padding-lg" wx:for="{{race.users}}" wx:key="userId" bindtap="unitDetail" data-user="{{item}}">
            <view class="flex align-center justify-between margin-bottom-sm">
                <view class="flex align-center">
                    <view class="cu-avatar round lg" style="background-image:url({{item.avatarUrl}});"></view>
                    <view class="padding-left-sm">
                        <view class="margin-bottom-sm">{{item.nickName}}</view>
                        <block wx:if="{{race.type === 0}}">
                            <view class="cu-tag bg-{{item.score>=60?'blue':'red'}} light round">分数: {{item.score}}</view>
                            <view class="cu-tag bg-blue light round">
                                答题次数: {{item.answerNum}}
                            </view>
                        </block>
                        <block wx:else>
                            <view class="cu-tag bg-red light round">抽奖次数: {{item.answerNum}}</view>
                        </block>
                    </view>
                </view>
                <view class="cuIcon-right text-lg" style="color:#ccc"></view>
            </view>
        </view>
    </view>
</view>
<view class="cu-bar bg-white tabbar border shop foot">
    <view class="btn-group">
        <button class="cu-btn lg bg-white flex-sub">返回</button>
        <button class="cu-btn lg bg-blue sp margin-right" open-type="share">分享活动</button>
    </view>
</view>