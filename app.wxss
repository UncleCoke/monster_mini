@import 'ui/main.wxss';
@import 'ui/icon.wxss';
@import 'ui/animation.wxss';

/* 避免页面切换图片模糊闪现 */
image {
  height: auto;
  vertical-align: middle;
}

/* 不显示按钮默认的上下线 */
button:after {
  border: none;
  outline: none;
}